<p style="color: green"><%= notice %></p>

<%= render @post %>

<div>
  <%= link_to "Edit", edit_post_path(@post) %>|
  <%= link_to "Back", posts_path %>

  <% if can? :destroy, @post %><%= button_to "Destroy this post", @post, method: :delete %><% end %>
</div>
